# Ops Run - 2026-05-24

## Purpose

Run the Stage A operations pass from the V2 scout plan:

- summarize research state
- check security
- verify the web UI integration surface
- classify failed queue jobs without mutating them
- run local construction audit

No jobs were rerun or changed during this ops pass.

## Checks Run

```bash
PYTHONPATH=src venv/bin/python -m tar_system.cli research-summary
PYTHONPATH=src venv/bin/python -m tar_system.cli security-check
PYTHONPATH=src venv/bin/python -m pytest tests/test_web_ui_integration.py
PYTHONPATH=src venv/bin/python -m tar_system.cli run-local-construction-audit --fail-on-findings
```

## Results

| Check | Result |
| --- | --- |
| Research summary | PASS |
| Security check | PASS, no findings |
| Web UI integration tests | PASS, 4 tests passed |
| Local construction audit | PASS, 0 findings |

## Research Summary

Current next actions reported by the system:

- Review 591 failed jobs before rerunning.
- Review best scored candidate: `gold_v2 XAUUSD M1 score=70.3`.
- Consider manual MT5 review gate for `gold_v2 BTCUSD H1`.

## Queue Health

Queue snapshot was written to:

```text
runtime/ops_queue_health_2026-05-24.json
```

Current queue state:

```text
COMPLETED: 188
FAILED: 591
QUEUED: 0
RUNNING: 0
SKIPPED: 392
TOTAL: 1171
```

Failed job buckets:

```text
failed_dashboard_or_fast_batch_no_result: 517
failed_no_result_path: 72
failed_no_completion_timestamp: 2
```

Failed by stage:

```text
continuous_all_strategies: 374
dashboard_batch: 136
full: 68
smoke: 8
paper_review: 3
none: 2
```

Failed by strategy:

```text
gold_v2: 143
rsi_reversion_v1: 142
goldv2_v2: 34
rsi_only_v3: 34
ema_volume_v3: 34
atr_breakout_v3: 34
momentum_crossover_v3: 34
multi_timeframe_v3: 34
ema_volume_fixed: 34
atr_breakout_fixed: 34
liquidity_sweep_v1: 34
```

## Interpretation

The system is safe to operate, but not ready for a broad rerun.

The largest failure group is dashboard/fast-batch style jobs that ended without
result paths. That looks like operating noise from previous broad queue runs,
not an active system break. Active queue is clean:

```text
QUEUED: 0
RUNNING: 0
```

## Next Operations

1. Add a compact queue health command/report.
2. Add a failure classifier that marks old failed jobs as one of:
   - stale_fast_batch
   - missing_result_path
   - incomplete_timestamp
   - rerun_candidate
   - manual_review
3. Do not rerun all failures.
4. Select a small manual review set:
   - `gold_v2 XAUUSD M1`
   - `gold_v2 BTCUSD H1`
   - XAUUSD M15 baseline/filter-tuning path
5. Move next to Stage B only after Stage A reporting is compact:
   - safe UI POST endpoints
   - queue paper research
   - queue paper signal
   - run all tests batch
   - request stop active task

## Decision

Ops pass is green. Proceed with queue-health tooling and safe UI write
endpoints. Avoid broad research reruns until failed jobs are classified.
