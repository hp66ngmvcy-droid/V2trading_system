# Local Performance
- Use Parquet after CSV import.
- Use DuckDB for queries.
- Cache repeated results.
- Precompute features.
- Avoid recomputing indicators.
- Keep pandas default.
- Optional Polars later only.
